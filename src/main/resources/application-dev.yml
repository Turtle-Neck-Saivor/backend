spring:
  jpa:
    hibernate:
      ddl-auto: create
      show_sql: true

  datasource:
    url: jdbc:h2:~/turtle-neck-savior-dev
    #    url: jdbc:h2:mem:turtle-neck-savior-dev;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
